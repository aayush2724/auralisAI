[tool.ruff.lint]
extend-select = ["I", "FURB", "PIE", "UP", "RUF100", "RUF022"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.params.Depends",
    "fastapi.File",
    "fastapi.Query",
    "fastapi.Path",
    "fastapi.Body",
    "fastapi.Header",
    "fastapi.Cookie",
    "fastapi.Form",
    "src.api.auth.require_roles",
]
